# Cloudflare Pages: SPA-роутинг. Без этого прямой заход на /blog/preview и любой
# другой внутренний маршрут отдаёт 404 — на Pages нет сервера, который вернул бы
# index.html. Правило ставим ПОСЛЕДНИМ: статика (assets, иконки) отдаётся как есть.
/*    /index.html   200
